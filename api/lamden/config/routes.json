{
  "routes": [
    {
      "method": "GET",
      "path": "/lamden/price",
      "handler": "lamden.getPrice",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/lamden/stamps",
      "handler": "lamden.getStamps",
      "config": {
        "policies": []
      }
    }
  ]
}
